[build]
target = "index.html"
dist = "dist"
public_url = "./"
filehash = true

[serve]
addresses = ["127.0.0.1"]
# Keep a predictable development URL; stop the previous Trunk run before
# starting another one so this port is released cleanly.
port = 8080
open = false
